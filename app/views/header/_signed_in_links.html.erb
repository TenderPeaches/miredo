<%# links shown to the user when they are signed in; as there is only one such link right now, it doesn't merit its own container and is just added to an existing one as necessary %>
<%= link_to t('content.settings.page_title'), user_settings_path %>