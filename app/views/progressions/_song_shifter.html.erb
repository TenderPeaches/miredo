<%= tag.div class: "song-shifter" do %>
    <% if song.id > 1 %>
        <%= link_to "<", song_progressions_path(song.id - 1) %>
    <% end %>
    <%= song.name %>
    <% if song.id < Song.last.id %>
        <%= link_to ">", song_progressions_path(song.id + 1) %>
    <% end %>
<% end %>