<%= turbo_stream.append "#{dom_id(@progression)}_chords" do %>
    <% ActionController::Base.helpers.fields model: @progression do |form| %>
        <% form.fields_for :progression_chords, @progression_chord do |progression_chord_form| %>
            <%= tag.fieldset class: "progression-chord-form" do %>
                <%= tag.legend t('actions.new', thing: t('models.progression_chord.model_name')) %>
                <%= render partial: "progression_chords/fields", locals: { form: form } %>
                
            <% end %>
        <% end %>
    <% end %>
<% end %>