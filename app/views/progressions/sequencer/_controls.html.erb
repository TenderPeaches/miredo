<%= tag.span class: "sequencer-buttons" do %>
    <%# https://www.toptal.com/designers/htmlarrows/arrows/up-paired-arrows/ %>
    <%= turbo_link_button "⇈", new_progression_sequence_adjustment_path(progression_id: progression.id, direction: :up, all: true, format: :turbo_stream) %>
    <%= turbo_link_button "↑", new_progression_sequence_adjustment_path(progression_id: progression.id, direction: :up, format: :turbo_stream) %>
    <%= turbo_link_button "↓", new_progression_sequence_adjustment_path(progression_id: progression.id, direction: :down, format: :turbo_stream) %>
    <%= turbo_link_button "⇊", new_progression_sequence_adjustment_path(progression_id: progression.id, direction: :down, all: true, format: :turbo_stream) %>
<% end %>