<% if @progression.valid? %>
    <%= turbo_stream.replace "#{dom_id(@progression)}__form" do %>
        <%= render "progressions/form", progression: @progression, song: @progression.song %>
    <% end %>
<% else %>
    <%= flash_model_errors @progression %>
<% end %>