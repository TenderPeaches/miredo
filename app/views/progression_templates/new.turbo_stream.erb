<%= turbo_stream.append "new-progression-templates" do %>
    <%# use random unique identifier to easily pluck this element without needing to rely on the progression's DB ID, which is not assigned yet (as a new but unsaved progression) %>
    <% uid = Time.now.to_i %>
    <%= tag.fieldset id: "new-progression-template_#{uid}" do %>
        <%= tag.legend t('actions.new', thing: t('models.progression_template.model_name')) %>
        <%= render "progression_templates/form", progression_template: @progression_template, uid: uid, song: @song %>
    <% end %>
<% end %>
<%= turbo_stream.remove "no-progression-templates" %>