<%= tag.nav do %>
    <%# "new progression templates" button %>
    <%= link_to t('actions.new', thing: t('activerecord.models.progression_template')), new_progression_template_path(song_id: song.id.to_s), data: { turbo_stream: true } %>
    <%# "edit song" button %>
    <%= link_to t('actions.edit', thing: t('activerecord.models.song')), edit_song_path(song) %>
    <%# "define song progressions" button %>
    <%= link_to t('cta.define_progressions'), song_progressions_path(song) %>
<% end %>