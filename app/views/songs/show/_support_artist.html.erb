<%= tag.div class: "support-artists" do %>
    <%= tag.div "#{t("content.show_song.support_artist")}!" %>
    <% artists.each do |artist| %>
        <%= artist_links(artist) %>
    <% end %>
<% end %>