<% if song.can_edit? current_user %>
    <%= tag.div class: "admin-actions" do %>
        <%= link_to t('cta.edit_song'), edit_song_path(song) %>
        <%= song_edit_progression_templates_link(song.id) %>
        <%= song_edit_progressions_link(song.id) %>
    <% end %>
<% end %>