<!-- to be used with songs whose structure is dictated by SongProgression entities, rather than plaintext -->
<fieldset>
    <legend>Key</legend>
    <%= @song.key %>
</fieldset>
<fieldset>
    <legend>Capo</legend>
    <%= @song.capo %>
</fieldset>