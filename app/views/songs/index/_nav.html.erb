<% if user_signed_in? %>
    <%= link_button_to "New Song", new_song_path %>
    <%= link_button_to "My Songs", songs_path(user_id: current_user) %>
<% end %>

<%= link_button_to "Random Song!", song_path(Song.random_id(current_user&.id)) %>