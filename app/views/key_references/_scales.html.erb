<%= tag.section class: "scales" do %>
    <%= tag.div id: "scales", class: "table scales-table" do %>    
        <%= scales_table_header %>
        <% scales.each do |scale| %>
            <%= render "key_references/scale", scale: scale %>
        <% end %>
    <% end %>
<% end %>